SNES Station v0.23 — Progress 9 validation

Target: official v0.23 WIP (24 Jan 2004) unpacked analysis image
Coverage proxy denominator: 1,137 heuristic direct JAL targets

Progress:
- MATCHING: 0 (0.00%)
- RECONSTRUCTED: 545 (47.93%)
- MAPPED: 610 (53.65%)
- renderer draw-family: 30/30 reconstructed

Validation:
- 65 src/**/*.c translation units pass:
  cc -std=c11 -Wall -Wextra -Werror -fsyntax-only -Iinclude
- Progress 9 mapped-memory/APU/CPU-shutdown/renderer host smoke test: PASS
- SyncDCache/iSyncDCache target extract checked for CP0 Status/TagLo,
  cache 0x10/cache 0x14, sync barriers, 0x40 stepping and DIntr/EIntr calls
- get/set target extract checked for 4 KiB block indexing, Map/WriteMap split,
  MemorySpeed accounting and 0xFFF word-crossing slow paths
- raw SetByte/SetWord jump tables re-read from target bytes; slot-6 asymmetric
  SA1RAM/SPC7110 behavior preserved and covered by the host smoke test
- GetWord boundary OpenBus ordering and S9xSetPCBase default-SRAM path covered
  by the host smoke test
- CPUShutdown target extract checked for WaitAddress/WaitCounter, flag mask 0x880,
  SA-1 dispatch and APU cycle/opcode catch-up loop
- renderer selector target function pointers resolve to already reconstructed
  tile/clipped/large-pixel families
- APU target extract checked for F0-FF ports/control/DSP/timer window and FFC0 shadow behavior
- direct-page IPL test verifies separate 64-byte ExtraRAM shadow with ShowROM on/off
- analysis/progress_targets.csv: 610 unique addresses, no duplicates
- analysis/symbols.csv: 610 unique addresses, no duplicates
- assets/progress.svg regenerated by tools/update_progress.py
- focused new target corridor ends at 0x001acd00 and joins WRITE_4PIXELS @ 0x001acd04
- existing contiguous renderer/helper tail returns at 0x001b07d0
- unpacked target bytes 0x001b07d4..0x001b087f are zero padding;
  data/string storage begins at 0x001b0880 with "%02d/%02d"

Progress 9 milestone:
- EE SyncDCache/iSyncDCache recovered
- S9xGetMemPointer/GetByte/SetByte/GetWord/SetWord/SetPCBase/GetBasePointer recovered
- CPUShutdown idle fast-forward and APU catch-up helper recovered
- SelectTileRenderer recovered and cross-linked to renderer entries
- S9xAPUGetByte/SetByte and Z direct-page variants recovered
- contiguous high-address code tail closed through its final helper return

Strict matching policy remains unchanged: reconstructed/identified source is not
MATCHING without a byte-identical candidate rebuild after relocation handling.
